# FreeGrid 使用说明

> 这份文档讲的是**怎么用**——装好之后每个功能怎么操作、屏幕上的数字怎么读。
> 想了解「这是什么 / 怎么下载安装 / 技术栈」，请看 [README](README.md)。

FreeGrid 只关心一件事：

> **按你现在的净值和花钱速度，你已经为自己买下了多少天的自由？**

这个数字叫 **自由天数**。它就是整个 App 的主角，下面所有功能都围绕它转。

---

## 一、先看懂一个数字：自由天数

```
日均消费   = 你记录以来的总支出 ÷ 记账天数
净每日消耗 = max(0, 日均消费 − 日均被动收入)
自由天数   = 净值 ÷ 净每日消耗      （净每日消耗为 0 时 = ∞）
```

一句话：**如果从今天起你不再赚一分钱，手头的钱够你按现在的节奏自由地活多少天。**

- 攒钱、收入到账 → 自由天数变多，网格点亮更多格。
- 花钱 → 自由天数变少，格子熄灭。
- 当**被动收入**（房租/股息/利息等不工作也有的钱）覆盖了你的日常消费 ≥ 100%，净值不再被消耗 → 自由天数变成 **∞**，App 显示「你已**财富**自由」。

> ⚠️ 自由天数不是凭空算的，它依赖「日均消费」。**刚装上、还没记几天账时，这个数字不准**——记得越久越接近你真实的花钱节奏。

---

## 二、第一次打开：冷启动三步

新装的 App 是**空的，所有数字都是 0**。按这三步把它「点亮」：

### ① 设置净值（Assets 标签页）

净值分成**两个桶**，这是 FreeGrid 的核心设计：

| 桶 | 含义 | 网格颜色 |
|---|---|---|
| **资产** | 锁定的、不会随手花掉的钱：投资、定期、理财 | 🟡 金色格子 |
| **现金** | 随时可花的活钱 | 🔵 蓝色格子 |

> 净值 = 资产 + 现金。**记录的收入默认进「现金」桶**；如果你把一笔钱转去投资/定期了，用「桶间调拨」把它从现金挪到资产。

进 Assets 页，点资产 / 现金卡片，把你现在手头的钱填进去。

### ② 记第一笔账（Dashboard 标签页）

回到 Dashboard，用底部的 **记支出 / 记收入** 按钮记账：

- 记**支出**：填金额 + 选分类（见 [第六节](#六支出分类只有这-9-类)）+ 日期。
- 记**收入**：填金额 + 来源（工资/副业等，自由填）+ 日期。收入默认进现金桶。

### ③ 看懂大数字

记了账、净值也填了之后，Dashboard 顶部就会显示**自由天数**。注意它会随大小**自动换单位**：

- 不足 1 年 → 显示**天**（`FREEDOM DAYS`）
- 1～10 年 → 显示**月**（`FREEDOM MONTHS`）
- 超过 10 年 → 显示**年**（`FREEDOM YEARS`）
- 被动收入全覆盖 → 显示 **∞**

> 记账头几天数字会跳得厉害（样本太少），属正常。坚持记一两周后趋于稳定。

---

## 三、四个标签页怎么用

### 📈 Dashboard（主面板）

你每天看得最多的一屏，从上到下：

- **自由天数大数字** —— 全局主角。
- **自由网格（Freedom Grid）** —— 把自由天数画成一格格方块。
  - 🟡 金色格 = 资产撑起的自由，🔵 蓝色格 = 现金撑起的自由（图例在网格下方）。
  - 格子粒度会**自动升档**：自由度低时一格 = 1 天，高了变 1 月、再高变 1 年（图例会注明「每格 = 1 X 自由」）。
  - 记一笔支出，对应格子会**级联熄灭**；记收入则**点亮**。
- **12 周趋势** —— 一条 sparkline + 涨跌标记（▲ +22d / ▼），告诉你最近是在「攒自由」还是「烧自由」，不只看今天。
- **今日 vs 日均** —— 今天花了多少、比你的日均高还是低，一眼看出今天是否超支。
- **记支出 / 记收入** 按钮 —— 高频操作放在拇指够得到的位置。
- **撤销** —— 刚记完的一笔，顶部会浮出 **5 秒撤销** 条；错过了也能去 History 删（见下）。

### 💰 Assets（资产）

管理「自由天数」的两个输入项——净值和被动收入：

- **净值双桶** —— 资产 / 现金两张卡，点进去改数字。
- **桶间调拨** —— 把现金挪进资产（如：拿活钱去买了基金），或反向。
- **被动收入源** —— 这是通往 ∞ 的关键：
  - 点 + 添加一个被动源（如「房租」），填**月收入**。
  - App 自动换算日均，算出**被动覆盖率** = 日均被动收入 ÷ 日均消费。
  - 覆盖率到 **100%** 时，自由天数变 ∞ —— 你的被动收入已经能养活日常的你了。
- **数据导入 / 导出** —— 见 [第五节](#五数据与备份重要)。

### 🕓 History（流水）

所有记录的明细：

- 顶部分段切换 **全部 / 支出 / 收入**。
- 支出页有**分类汇总横滑条**，点某个分类只看该类流水，再点取消。
- 每行右侧有个 **×**，点它撤销这笔。撤销前会**弹窗确认**，明确告诉你「这笔删掉后，现金会反向 +/− 多少钱」，不会误删。
- 导航栏可进 **月度汇总**：每个月的总支出 / 总收入 / 净额，点开看当月各分类支出的占比。

### ✅ Check（自检）

8 项「财富自由」自检清单，**全部根据你的记录自动判断，不用手动打勾**：

1. 记录天数超过 30 天
2. 了解自己的日均消费（记满 7 天且有支出）
3. 记录了可变现资产（净值 > 0）
4. 自由天数超过 180 天
5. 自由天数超过 365 天
6. 有被动收入来源
7. 被动覆盖率超过 50%
8. 被动收入覆盖日常消费（≥100%）

顶部大数字显示你已达成几项（N/8）。这是一条从「开始记账」走到「财富自由」的路线图。

---

## 四、模拟决策：买之前先预演

这是 FreeGrid 的招牌功能——**下单之前，先看这笔钱会让你的自由少几天。**

入口：Dashboard 上的 **「模拟一笔 · 看决策影响」**。

- 切换 **模拟支出 / 模拟收入**，填一个金额。
- **顶部明确标注「模拟模式，不会真实记账」**——随便试，不写进数据库。
- **戴维斯三杀预览**：实时显示这笔操作对
  - `KILL 1 净值` / `KILL 3 自由天数`（支出）
  - `GAIN 1 净值` / `GAIN 2 自由天数`（收入）

  的传导效应——比如「这笔 −16 天」。
- **格子推演动画**：把「−16 天」翻译成肉眼可见的网格级联熄灭 / 点亮，让你「感觉到」这笔花出去的代价。

用它来回答那个永恒的问题：**这笔，到底买不买。**

---

## 五、数据与备份（重要）

FreeGrid **没有云、没有账号、不联网**，你的数据只存在这一台手机里。这是隐私的保证，也意味着：

> **删了 App，数据就没了。导出 JSON 是你唯一的备份通道。**

在 **Assets 页底部** 操作：

- **导出 CSV** —— 用 Excel / Numbers 打开做分析，给你一份可读的流水表。
- **导出 JSON** —— **完整备份**。这是能原样导回的格式，换手机 / 重装前务必导一份。
- **从 JSON 导入** —— 两步式，不会瞎覆盖：
  1. 先弹**预览**：告诉你将导入多少笔。
  2. **分类对齐**：外来数据里不标准的分类（如 `transport`、`数码`），App 会建议归到标准 9 类，拿不准的标橙让你确认。
  3. **净值落桶策略**：导入文件里的净值，你来决定是「替换 / 加进现金 / 跳过」，不会偷偷盖掉你已分好的桶。
- **清空所有数据** —— 在底部，弱化处理（防误触）。点了会二次确认。

> 💡 建议：养成**定期导出 JSON** 的习惯（比如每月一次，存到 Files / iCloud Drive / 发给自己）。这是你和数据之间唯一的安全绳。

---

## 六、支出分类只有这 9 类

手动记账时，支出分类是固定的 9 类（保证后续统计干净）：

> 早餐 · 午餐 · 晚餐 · 购物 · 交通 · 娱乐 · 成长投资 · 医疗 · 其他

（收入的「来源」则是自由文本，随便填。）从别处 JSON 导入时出现的其它分类，会在导入那一步被归一到这 9 类。

---

## 七、浅色 / 深色主题

FreeGrid 有两套配套设计（Silverline）：浅色冷白银、深色天文台冷蓝紫（深色 hero 卡里还有流星动画）。**跟随系统外观自动切换**，无需手动设置。

---

## 八、故障排查 FAQ

**Q：装好后过了几天，App 打不开 / 闪退了？**
A：这是 iOS 系统限制，不是 App 的 bug。用**免费 Apple ID** 自签的 App **7 天会过期**，需要重新签名。用 AltStore 且手机和电脑在同一 WiFi 下，它能后台自动续签；用 Sideloadly 则需到期后重新签一次。详见 [最新 Release 说明](https://github.com/coni555/FreeGrid-Freedom/releases/latest)。

**Q：系统切了深色，App 图标却没变深色？**
A：先别怀疑 App。多半是 iOS 主屏的图标外观开关被锁住了：**长按主屏空白处 → 点左上「编辑」→ 自定 → 顶部「自动 / 深色 / 浅色 / 着色」开关**，如果锁在「浅色」，会强制所有图标都用浅色、无视系统深色。切到「自动」即恢复。

**Q：自由天数一直是 0 / 不显示 / 不动？**
A：它需要两个前提才有意义：① **净值 > 0**（去 Assets 填资产/现金）；② **记过几天账**（日均消费才算得出来）。两样都有了，数字就出来了。

**Q：我的自由天数和我心算的不一样？**
A：它不是「净值 ÷ 月开销」那么简单。分母是 **日均消费 − 日均被动收入**，而日均消费是你**记账以来的平均值**——记得越久越平滑，单笔大额支出会暂时拉高它、压低自由天数，之后慢慢摊平。有被动收入时，分母还会再减。

**Q：我的数据会不会被上传 / 被谁看到？**
A：不会。这个版本**整个代码里没有任何网络请求**，数据从不离开你的设备，没有账号、没有埋点、没有第三方 SDK。代价就是你得自己导出 JSON 做备份（见第五节）。

**Q：怎么换手机 / 重装不丢数据？**
A：旧机 **导出 JSON** → 存到 Files / iCloud Drive / 发给自己 → 新机装好 App → **从 JSON 导入**。

---

> 还有问题，或想反馈 / 提需求？欢迎到 [FreeGrid-Freedom 仓库](https://github.com/coni555/FreeGrid-Freedom) 提 issue。
