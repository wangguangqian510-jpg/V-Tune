// Apple Music 大屏播放界面已合并进 NowPlayingView ── AudioPlayerService
// 在 Apple Music 模式下镜像 ApplicationMusicPlayer 状态, NowPlayingView 把
// 不适用的功能 (EQ / 刮削 / 播放速度等) 按 player.isAppleMusicMode 隐掉。
// 文件保留是因为 pbxproj 仍引用; 后续清理 build 后删除。
